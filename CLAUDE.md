# variance (가제) 작업 메모

새 세션을 시작하면 아래 "진행 상황 로그"부터 읽고 이어서 작업할 것.
세션을 마칠 때는 맨 위에 날짜와 함께 새 항목을 추가한다 (무엇을 했는지 / 다음에 할 일).

---

## 이 저장소가 무엇인가

**동물의숲 × 타이푼(홀덤펍 경영)** 을 합친 **2D 픽셀 경영·꾸미기 게임.**
작은 동네 펍에서 시작해 세계대회를 여는 곳까지 키운다.

- 재미의 축 4개: **① 딜러 교육 ② 기물 구매·업그레이드 ③ 추가 콘텐츠(대회·이벤트·손님 서사) ④ 스킨·디자인으로 나만의 펍 꾸미기**
- 시점: **탑다운 2D 픽셀**(동물의숲식 살짝 기울어진 위에서 보기). 아이소메트릭이 아니다.
- 빌드 도구 없음. `index.html`을 그대로 열면 돌아간다. GitHub Pages로 배포한다.
- 저장: `localStorage` (키 `variance.save.v1`). 온라인 기능은 나중.

관련 저장소(코드 공유 안 함, 교훈만 이관): `holdem-pub-tycoon`(3D+아이소, 원조), `holdem-pub-iso`(아이소 렌더러 분리본).

## 저장소 규칙

- **커밋·푸시 자동.** 의미 있는 작업 단위마다 `git add -A && git commit && git push origin main`. 묻지 않는다.
- **공개 저장소.** 비밀(키·토큰·.env)은 절대 커밋하지 않는다. `.gitignore`가 막고 있다.
- 원격: https://github.com/thstjdals09-lang/variance-pub · Pages: https://thstjdals09-lang.github.io/variance-pub/
- 생성 원본 이미지는 `assets/raw/`(gitignore). 잘라서 구운 결과만 `assets/pack/<테마>/`에 커밋한다.
- 커밋 메시지는 한국어, 한 줄 요약 + 필요하면 본문.

## 폴더 구조

```
index.html        진입점 (캔버스 + HUD)
css/style.css     HUD·패널 스타일
js/data.js        수치표: 티어, 기물, 딜러, 업그레이드, 스킨 (순수 데이터)
js/state.js       세이브/로드, 게임 상태 한 덩어리
js/sim.js         경제 틱: 손님 유입·수익·딜러 성장 (렌더러와 분리)
js/render.js      픽셀 렌더러: 타일맵, 기물, 사람, 카메라
js/ui.js          HUD·상점·배치 모드 패널
js/main.js        부팅, 루프
assets/pack/<테마>/   구운 스프라이트 + manifest.json
assets/raw/           생성 원본 (로컬만)
tools/                굽기·검증 스크립트 (node, 브라우저 없이)
docs/design.md        기획 문서 (티어·콘텐츠·경제 설계)
```

## 절대 다시 밟지 말 것 (이전 저장소에서 값을 치르고 얻은 규칙)

### 에셋 크기
- **크기는 미터로 적는다.** 스펙에 `[가로 m, 세로 m, 높이 m]`. 타일 1칸 = 1m = 16px. 사람 1.6m ≈ 24px(2등신).
  "긴 변 몇 px"이나 눈대중 높이로 주면 문이 사람 두 배가 되고 의자가 사람보다 커진다.
- **사람은 시트 한 장에서 배율을 한 번만 구해 전 칸에 적용한다.** 칸마다 자기 바운딩 박스를 맞추면 팔 든 그림은 작아지고 앉은 그림은 커진다.
- 앉은 칸은 높이 상한을 반드시 건다. 2등신은 앉아도 박스가 안 줄어든다.

### 시트 생성 (이미지 생성 도구 쓸 때)
- **낱개로 뽑지 말고 한 장에 12칸씩(4×3) 뽑는다.** 낱개는 시점·광원·외곽선·팔레트가 조금씩 달라진다.
- **개수와 열 수를 문장 맨 앞에 STRICT로 못박는다.** 안 그러면 4×4로 그려 오거나 절반 해상도로 온다.
- **머리카락을 명시한다.** 2등신만 강조하면 전원 대머리로 나온다.
- **뒷모습은 별도 시트로.** 머리를 옷보다 밝게, 목덜미 선, 어깨 각을 명시.
- **한 칸에 물건 하나.** 테이블 칸에 의자·칩을 같이 그리게 두면 배치에서 의자가 중복된다.
- 배경은 마젠타(키잉용), 3톤, 어두운 외곽선, 좌상단 광원으로 통일.
- 간판 글씨는 생성에 맡기지 않는다. 코드에서 픽셀 폰트로 찍는다.
- 테마 모티브는 분위기만 쓴다. 작품명·스튜디오·캐릭터 이름을 프롬프트에 넣지 않는다.
- 줄 수 판정은 "맞는 격자에서는 어떤 성분도 칸 경계를 넘지 않는다"로 자동 판별한다.
- 동시 생성은 6장쯤에서 429가 난다. 나눠 던진다.

### 렌더링 (탑다운)
- 그리기 순서는 **y(발 위치) 정렬**. 벽 앞 가구·사람이 자연스럽게 겹친다.
- 바닥 접지는 함수 하나(`blit(key, footX, footY)`)로. 물건별 보정값을 주지 않는다.
- 배치 모드는 **격자 스냅 + 충돌 지도**. 사람 경로는 막힌 칸을 피해 고른다(직선 고정 경로 금지).
- 캔버스는 정수 배율(`image-rendering: pixelated`)로만 키운다. 소수 배율은 픽셀이 뭉개진다.
- 앞벽이 없으면 "건물 안"으로 안 보인다. 벽·바닥 색을 같게 두지 않는다.

### 작업 습관
- **크레딧 쓰기 전에 코드로 되는지 먼저 본다.** 크기·방향 문제는 대부분 굽는 단계와 렌더러 버그였다.
- 고칠 때마다 브라우저 없이 한 프레임 찍는 검증 스크립트(`tools/preview.mjs`, 추후)로 확인한다.
- 사용자가 화면을 보고 지적한 것은 로그에 "지시:"로 적고 원인을 같이 적는다.

---

## 진행 상황 로그

### 2026-09-19 (1차) — 저장소 생성, 뼈대

- 저장소 생성(public), 자동 커밋·푸시 규칙 확정, Pages 배포.
- 뼈대: 탑다운 픽셀 렌더러(코드 그린 임시 스프라이트), 경제 틱(테이블·딜러·손님), 세이브, HUD(상점·딜러·꾸미기 탭), 배치 모드.
- 기획 문서 `docs/design.md` — 티어 7단계(동네 펍 → 세계대회), 콘텐츠 축 4개, 경제 뼈대.
- **다음 할 일**: 첫 테마 에셋 시트 뽑기(기물 12 + 사람 12) → `tools/pack-build.mjs` 굽기 → 코드 스프라이트 교체. 딜러 교육 미니게임 설계.
